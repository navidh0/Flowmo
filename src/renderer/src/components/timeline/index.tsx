/**
 * Entry point for the timeline surface.
 *
 * The default export exists so integration can lazy-load this screen:
 *
 *   const TimelinePage = React.lazy(() => import('@renderer/components/timeline'))
 */

export { TimelinePage as default, TimelinePage } from './TimelinePage'
